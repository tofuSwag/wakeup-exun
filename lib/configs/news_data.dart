class NewsData {
  NewsData({this.title, this.description, this.author, this.tag, this.imageUrl, this.url});

  String title;
  String description;
  String author;
  String tag;
  String url;
  String imageUrl;
}